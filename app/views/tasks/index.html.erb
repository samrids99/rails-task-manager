<h1>My tasks</h1>
<ul>
  <% @tasks.each do |task| %>
    <li><%=link_to task.title, task_path(task) %></li>
    <%= link_to "Delete", task_path(task.id), data: {turbo_method: :delete} %>
  <% end %>
</ul>
<p><%= link_to "create a new task", new_path %></p>
